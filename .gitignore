.DS_Store
.claude/settings.local.json

# secrets — this repo is public, never commit these
.env
.env.*
!.env.example

# scratch: logo design exploration, not part of the published site
logo-exploration.html
.vercel
